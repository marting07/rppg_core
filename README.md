# rppg_core

Shared Python package containing rPPG method implementations (Green, CHROM, POS, SSR),
common preprocessing, and ROI helpers.

## Purpose

This package is the shared source of truth consumed by:

- `rppg_method_benchmark` (offline method comparison track)
- `rppg_live_backend_service` (live liveness verification track)
